import React from 'react';
import { View, Text } from 'react-native';
import BottomTabs from '../navigation/BottomTabs'; // Adjust the path as needed

const PlusScreen = () => {
  return (
    <View>
     
      <Text>Welcome to the Settings Screen!</Text>
    </View>
  );
};

export default PlusScreen;